{
  "name": "monk10x-newsletter-trigger",
  "version": "1.0.0",
  "private": true,
  "description": "Trigger.dev scheduling for the Monk10x newsletter automation (the actual pipeline is Python, in tools/ - see trigger.config.ts and src/trigger/newsletter.ts)",
  "scripts": {
    "dev": "npx trigger.dev@latest dev",
    "deploy": "npx trigger.dev@latest deploy"
  },
  "dependencies": {
    "@trigger.dev/python": "4.6.2",
    "@trigger.dev/sdk": "4.6.2"
  },
  "devDependencies": {
    "@trigger.dev/build": "4.6.2",
    "typescript": "^5.6.0"
  }
}
